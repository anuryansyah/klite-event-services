const authController = require("./auth");
const userController = require("./user");
const roleController = require("./role");

module.exports = {
  authController,
  userController,
  roleController
};
